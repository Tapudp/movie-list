// when page limit has been reached
export const MAX_PAGE_LIMIT_REACHED = 'You have seen all the available pages.';

// when there is an error while fetching the page data
export const PAGE_FETCH_ERROR = 'There was an error while fetching the page';
